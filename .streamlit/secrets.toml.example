# Copy this file to .streamlit/secrets.toml for local development.
# .streamlit/secrets.toml is gitignored — never commit real keys.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"

# Recommended: without this set, anyone who opens the app's base URL can
# start a session or unlock facilitator controls (reset board, trigger
# the summary). Set a password here to require it.
FACILITATOR_PASSWORD = "choose-a-password"

# Recommended: gates the N = Everyone Results page (pages/2_Survey_Results.py).
# This is not the survey link itself — respondents never see or need this —
# it's for whoever's reviewing tabulated results and open-ended responses.
# Without it set, that page is open to anyone with its URL.
SURVEY_RESULTS_PASSWORD = "choose-a-different-password"

# Optional: prefills the "Deployed app URL" field in the facilitator
# sidebar (used to build the QR code / join link). You can also just
# paste it in manually each session.
APP_BASE_URL = "https://your-app-name.streamlit.app"

# Optional: override the default model used for the closing summary.
# ANTHROPIC_MODEL = "claude-sonnet-5"
